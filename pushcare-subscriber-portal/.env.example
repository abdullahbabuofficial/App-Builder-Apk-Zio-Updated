# Where the PushCare API lives. The portal POSTs preferences here.
# VITE_PUSHCARE_API_URL=http://localhost:8787

# Branding (optional — server typically tells us the app name from the token).
# VITE_BRAND_NAME=PushCare
