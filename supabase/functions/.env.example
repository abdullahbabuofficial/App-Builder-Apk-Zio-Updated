# Local reference only — Edge Functions do not load this file in production.
# Set secrets in Supabase Dashboard (Edge Functions → Secrets) or via CLI.
# See docs/EDGE_SECRETS.md for semantics.

# --- team-invite ---
# INVITE_EMAIL_MODE=stub
# INVITE_EMAIL_MODE=resend
# INVITE_APP_BASE_URL=https://admin.example.com
# RESEND_API_KEY=re_xxxxxxxx
# INVITE_EMAIL_FROM=Invites <invites@example.com>
# INVITE_EMAIL_SUBJECT=You're invited to Apkzio

# --- webhook-deliver ---
# WEBHOOK_SIGNING_SECRET=generate-a-long-random-string
