// Top-level build file. The ApkZio WebView Builder generated this file at __GENERATED_AT__.
// This template ships only the source tree — Gradle is not invoked by the local-api dev
// pipeline. After unzipping, run `gradle wrapper` once and then open the project in
// Android Studio (File > Open) to finish the import.

plugins {
    alias(libs.plugins.android.application) apply false
    alias(libs.plugins.kotlin.android) apply false
}
