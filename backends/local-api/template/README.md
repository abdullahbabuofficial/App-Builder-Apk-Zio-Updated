# __APP_NAME__

This Android Studio project was generated by the **ApkZio WebView Builder** at
`__GENERATED_AT__`. It is a minimal WebView host that loads `__START_URL__` and exposes
a few configurable knobs (pull-to-refresh, swipe-back, file uploads, geolocation,
camera, offline fallback).

> The local-api dev pipeline does **not** run Gradle. It only emits this clean source
> tree as `source.zip`. To build an APK locally:
>
> 1. Unzip the archive.
> 2. From the project root, run `gradle wrapper` once to generate `gradlew` /
>    `gradlew.bat` / `gradle/wrapper/gradle-wrapper.jar`.
> 3. Open the folder in **Android Studio** (Hedgehog or newer) and let it sync.
> 4. Press **Run**.

## Configuration knobs (already baked in)

| Setting | Value |
|---------|-------|
| Application id | `__PACKAGE_NAME__` |
| Version | `__VERSION_NAME__` (`__VERSION_CODE__`) |
| Start URL | `__START_URL__` |
| Primary color | `__PRIMARY_HEX__` |
| Background color | `__BACKGROUND_HEX__` |
| Splash color | `__SPLASH_HEX__` |
| Pull-to-refresh | `__PULL_TO_REFRESH__` |
| Swipe-back | `__SWIPE_BACK__` |
| File uploads | `__ALLOW_FILE_UPLOADS__` |
| Geolocation | `__ALLOW_GEOLOCATION__` |
| Camera | `__ALLOW_CAMERA__` |

The launcher icon is a generic placeholder vector — replace
`app/src/main/res/drawable/ic_launcher_foreground.xml` with your real artwork.
