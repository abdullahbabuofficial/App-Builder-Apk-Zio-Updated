PushCare debug keystore placeholder.
We do NOT ship a real debug key in source control.
Regenerate per tenant with `keytool` before `assembleRelease`.
See ../README.md and the apk-builder README "Signing" section.
